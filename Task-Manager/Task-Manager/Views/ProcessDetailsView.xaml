<UserControl x:Class="Task_Manager.Views.ProcessDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Task_Manager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="Black" BorderThickness="0.5"/>
        <Border Grid.Row="1" BorderBrush="Black" BorderThickness="0.5"/>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="Selected Process Details" Style="{StaticResource HeaderTextBlockStyle}" />
            <Button x:Name="killProcessBtn">Kill</Button>
            <Button x:Name="stopRaefreshBtn">Change Priority</Button>
            <ComboBox Name="cmbPriorities"/>
        </StackPanel>
    </Grid>
</UserControl>
